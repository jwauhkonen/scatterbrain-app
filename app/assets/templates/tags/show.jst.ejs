<h1 class="title"> Most <%= tag.get('name') %> lyrics</h1>

<div class="panel panel-default">
	<table class="table">
		
		<thead>
			<tr>
				<th>Quote</th>
				<th>Song</th>
				<th># Tags</th>
			</tr>
		</thead>
		
		<tbody>
			<% tag.segments().each(function(segment) { %>
				<tr>
					<td class="index-segment"><a href="#/songs/<%= segment.get('song_id') %>"> <%= segment.get('quote') %></td>
					<td><a href="#/songs/<%= segment.get('song_id') %>"> <%= segment.get('song_title') %></td>
					<td><%= segment.taggingsByTagId(tag.id).length %></td>
				</tr>
			<% }); %>
		</tbody>
		
	</table>
</div>